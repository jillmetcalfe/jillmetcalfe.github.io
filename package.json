{
  "name": "jillmetcalfe-website",
  "version": "2.0.0",
  "private": true,
  "description": "jillmetcalfe.com — markdown from Notion, built into static HTML by one script.",
  "scripts": {
    "sync": "node sync.js",
    "build": "node build.js",
    "start": "node build.js && node serve.js",
    "publish": "node sync.js && node build.js"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2",
    "notion-to-md": "^3.1.1"
  }
}
