# The built website — regenerated by `npm run build`, never committed
site/

# Installed packages — reinstalled by `npm install`
node_modules/

# macOS clutter
.DS_Store

# Local secrets — never commit this
.env
